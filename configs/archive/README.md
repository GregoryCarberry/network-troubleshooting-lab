# Archive

Legacy configuration artifacts preserved for reference.

These files are **not** the current baseline and may reflect earlier lab phases.

All secrets should be removed or replaced with `<REDACTED>`.
