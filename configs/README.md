# Configuration Baselines

This directory contains redacted baseline configurations used in the lab.

These are:

- Known-good recovery states
- Sanitised for public release
- Used as rollback references after experimentation

Sensitive information (passwords, keys, serial numbers) has been removed.

Experimental configurations are not stored here.